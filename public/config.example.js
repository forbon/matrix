// Copy this file to config.js and fill in your URLs.
// config.js is gitignored and never committed.
//
// Two ways to point to your legal pages:
//   1) External hosting — full URLs (e.g. on a separate domain or wiki):
//        impressumUrl: 'https://example.de/impressum',
//   2) Self-hosted alongside the app — mount the HTML files into the
//      container (see README, Docker section) and use relative paths:
//        impressumUrl: '/impressum.html',
//
// If a field is empty or omitted, the corresponding footer link is hidden.
window.__MATRIX_CONFIG__ = {
  impressumUrl: 'https://example.de/impressum',
  privacyUrl: 'https://example.de/datenschutz',
};
